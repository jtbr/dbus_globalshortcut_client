{
  "name": "dbus-portal-client",
  "version": "0.1.0",
  "private": true,
  "description": "Dependency-free D-Bus client for the XDG GlobalShortcuts portal",
  "type": "commonjs",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
